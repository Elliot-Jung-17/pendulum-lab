# Pendulum Lab 재설계 실행 폴더

이 폴더는 기존 Pendulum Lab을 보존하면서 새 `배우기`와 `실험실` UI로 교체하기 위한 source of truth다.

## 파일

- `master-roadmap-ko.md`: 제품 구조, 보존 범위, 목표 아키텍처, 품질 기준
- `execution-plan-ko.md`: 실제로 순서대로 실행할 30단계
- `curriculum-map-ko.md`: 8개 과정·86개 단원의 범위와 전용 실험
- `status.json`: 완료된 단계와 다음 단계

## 새 대화에서 실행하는 방법

이 저장소의 `codex/redesign` 브랜치를 연 상태에서 다음처럼 입력한다.

```text
1단계 실행해줘.
```

또는:

```text
재설계 1단계 실행해줘.
```

루트 `AGENTS.md`가 Codex에 이 폴더의 모든 계획과 상태를 먼저 읽고 해당 단계만 수행하도록 지시한다. 단계가 성공하면 Codex는 필요한 검증을 실행하고, 상태 파일을 갱신하고, 단계별 커밋을 만든 뒤 `origin/codex/redesign`에 push한다.

## 중요한 사용 규칙

- 번호를 건너뛰지 않는다.
- 한 요청에서는 한 단계만 실행한다.
- 실패한 단계는 같은 번호로 다시 실행한다.
- 새 대화가 다른 브랜치에서 열렸다면 먼저 `codex/redesign`인지 확인한다.
- `master` merge는 30단계 완료 후 별도로 승인한다.
- Landing Page는 이 계획에서 수정하지 않는다.

## 현재 계획 버전

- 계획: v2
- 실행 단계: 30개
- 학습 과정: 8개
- 학습 단원: 86개
- 기능 잠금: 없음
- 신규 저장소 생성: 없음
