/**
 * @packageDocumentation
 *
 * `analysis` — chaos diagnostics: Lyapunov exponents/spectra (with block
 * uncertainty), RQA, basin/Wada analysis, CLVs, FTLE fields and ridges,
 * Floquet/continuation/branch switching, Melnikov thresholds, the 0–1 test,
 * recurrence networks, Neimark–Sacker detection, codim-2 scans, shadowing,
 * and Poincaré section tooling.
 *
 * Every estimator reports method, transient handling, and uncertainty so
 * results can be quoted with caveats.
 */

export * from '../chaos';
